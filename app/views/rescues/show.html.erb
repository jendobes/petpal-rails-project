<%= image_tag @rescue.owner.image.url(:medium) %> <%= image_tag @rescue.pet.image.url(:medium) %>
<br>
The story of <%=link_to @rescue.owner.name, owner_path(@rescue.owner)%> and <%=link_to @rescue.pet.name, pet_path(@rescue.pet)%>:
<br>
<%=@rescue.story%>
<br>

<% if session[:user_id] == @rescue.owner.id %>
<%= link_to 'Edit', edit_owner_rescue_path(session[:user_id], @rescue) %>
<%= link_to "Delete", owner_rescue_path(session[:user_id], @rescue), method: :delete, data: { confirm: "You sure?" } %>
<%end%>

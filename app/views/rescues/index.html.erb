<% @rescues.each do |r| %>
  <%= link_to(rescue_path(r)) do %>
    <%= r.owner.name %> & <%= r.pet.name %>
  <%end%>
  <br>
<% end %>
<br>
<%= link_to 'Add a new rescue story', new_owner_rescue_path %>
